{
  "name": "cv-hub",
  "version": "1.0.0",
  "description": "Small local CV hub with HTML/CSS CV storage, shared profile picture upload and PDF export.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.45.0"
  }
}
