{
  "name": "cv-hub-app",
  "version": "1.0.0",
  "description": "Local CV hub to manage HTML/CSS resumes and export them as PDF.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && npx playwright install chromium",
    "setup:linux": "npm install && npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.49.0"
  }
}